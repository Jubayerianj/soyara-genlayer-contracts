# Build artifacts and caches
out/
cache/
cache_forge/
artifacts/
typechain/
typechain-types/
solidity-files-cache.json
broadcast/*/*/dry-run/
__pycache__/
*.pyc

# Dependencies
node_modules/

# Secrets. Never commit a key.
.env
.env.*
!.env.example
*.pem
*.key
id_rsa*

# This repository is CONTRACTS ONLY.
# The frontend, the marketing site and example integrations live in the product
# repository; carrying them here is what made the contract artifact hard to
# review on its own.
frontend/
soyara-website/
using-sdk-something-project/
sdk/

# OS
.DS_Store
*.swp
